'use strict';const express=require('express');const cors=require('cors');const helmet=require('helmet');const path=require('path');require('dotenv').config({path:path.join(__dirname,'..','.env')});if(!process.env.JWT_SECRET||process.env.JWT_SECRET.length<32)throw new Error('JWT_SECRET must contain at least 32 characters');const app=express();const allowedOrigins=(process.env.ALLOWED_ORIGINS||'').split(',').map((value)=>value.trim()).filter(Boolean);app.use(helmet());app.use(cors({credentials:true,origin:(origin,callback)=>(!origin||allowedOrigins.includes(origin))?callback(null,true):callback(new Error('origin not allowed'))}));app.use(express.json({limit:'2mb'}));const {authenticateToken}=require('./middleware/auth');app.get('/api/health',(_req,res)=>res.json({status:'ok',service:'governed-music-creation'}));app.use('/api/auth',require('./routes/auth'));app.use('/api/governed-creation',require('./routes/governedCreation')(authenticateToken));app.use('/api',(_req,res)=>res.status(404).json({error:'Not found'}));const port=Number(process.env.SERVER_PORT||3001);app.listen(port,()=>console.log(`Governed music API listening on ${port}`));
